services:
  postgres:
    image: postgres:16-alpine
    container_name: verisure_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-verisure_db}
      POSTGRES_USER: ${POSTGRES_USER:-verisure_app}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-verisure_secure_pass_2026}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-verisure_app} -d ${POSTGRES_DB:-verisure_db}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:

